#include "converters.h"

size_t convert_md_to_pdf(const char *in_data,
                         size_t in_data_len,
                         char **out_data)
{
    // Utilise pdf_utils and markdown_utils
    // to do a conversion
    return 0;
}
